import React from 'react';
import { StyledActionPageContainer } from './styles';

const ActionPageContainer = ({ children }) => {
  return <StyledActionPageContainer>{children}</StyledActionPageContainer>;
};

export default ActionPageContainer;
